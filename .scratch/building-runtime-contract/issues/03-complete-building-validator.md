# 03 — 完成严格的地点档案运行时校验

**要实现的内容：** 数据维护者可以把未知输入交给唯一的纯校验边界，并一次获得稳定、可定位的全部问题；合法数据被转换为可信地点档案，非法字段和值会在命令行验收中被拒绝。

**前置任务：** 02 — 从运行时地点档案中删除访古等级

**状态：** complete

- [x] 公共 validator 接收 `unknown`，成功时返回完整可信的地点档案集合，失败时返回按确定规则排列的问题列表。
- [x] 正式运行时记录及图片对象拒绝未知字段，因此重新出现的 `tier` 和拼错的字段均会失败；source 与 intermediate 数据不受此规则限制。
- [x] 集合形态、非空集合、唯一正整数 ID、必填与可空字段、字符串与整数约束均符合已批准 spec，且不固定 532 条或要求 ID 连续。
- [x] 城市、遗产类型、简介来源和坐标精度使用严格枚举；朝代、地址、县区、批次编号和坐标来源等展示或来源文字保持开放。
- [x] 坐标必须是有限数字并落在记录于事实文档的宽松山西范围内；错误信息明确说明范围通过不代表坐标准确。
- [x] 营造学社描述与来源必须成对出现；Wikipedia 和图片 URL、图片许可与署名满足已批准的跨字段约束。
- [x] `rating`、`tel`、`is_open` 仅兼容当前已批准形态，包含对字符串评分和空数组评分的显式过渡测试，不借此设计最终实用信息 schema。
- [x] 测试覆盖有效完整记录、兼容空值、错误 primitive、未知字段、重复或非法 ID、越界与写反坐标、非法枚举、URL、图片和营造学社配对。
- [x] 命令行继续调用同一个 validator；真实 artifact 通过，人工非法输入产生可定位输出和非零退出码。
- [x] 本地聚合检查与生产构建通过。

## 验收证据

- `npm run check`：0 error，6 个既有 warning；27 项测试通过；真实 runtime artifact 的 532 条记录通过完整校验。
- `npm run build`：通过，并生成全部 537 个静态页面。
- CLI 集成测试：人工非法候选同时报告未知 `tier` 和缺失字段，并以状态码 1 退出。
- 双轴审查（固定点 `8e05091`）：Spec 轴 0 项问题；Standards 轴 0 个硬性问题，唯一的重复代码判断已通过共享未知字段 helper 解决并复验。
- 本 ticket 未修改 `src/data/buildings.json`，也未提前实现 Ticket 04 的应用加载接线。
